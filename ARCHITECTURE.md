# 🏗️ بنية المشروع - Money Manager

## نظرة عامة

تطبيق **Money Manager** هو نظام إدارة مالية شامل مبني باستخدام:
- **Next.js 15.5.4** مع App Router و Turbopack
- **React 19.1.0** مع TypeScript
- **Supabase** لقاعدة البيانات PostgreSQL والمصادقة
- **Zustand** للـ State Management
- **Tailwind CSS 4** للتصميم

---

## 📁 هيكل المجلدات

```
src/
├── app/                    # Next.js App Router
│   ├── (auth)/            # صفحات المصادقة
│   ├── (dashboard)/       # صفحات لوحة التحكم
│   └── layout.tsx         # Layout الرئيسي مع Providers
│
├── components/            # مكونات React
│   ├── ui/               # مكونات UI الأساسية (shadcn/ui)
│   ├── forms/            # نماذج الإدخال
│   └── dashboard/        # مكونات لوحة التحكم
│
├── contexts/             # React Contexts (20 context)
│   ├── cards/           # البطاقات الائتمانية (مُقسّم)
│   ├── prepaid-cards/   # البطاقات المدفوعة مسبقاً (مُقسّم)
│   └── provider-groups.tsx  # تجميع Providers
│
├── hooks/               # Custom Hooks
│   └── use-crud.ts     # Generic CRUD Hook
│
├── lib/                 # Utilities
│   ├── supabase.ts     # Supabase Client
│   ├── data-transformers.ts  # تحويل البيانات
│   └── error-handler.ts      # معالجة الأخطاء
│
├── stores/             # Zustand Stores (16 store)
│   └── StoreInitializer.tsx  # تهيئة الـ Stores
│
└── types/              # TypeScript Types
```

---

## 🔄 تدفق البيانات

```
┌─────────────────────────────────────────────────────────────┐
│                        Supabase                              │
│  (PostgreSQL + Real-time Subscriptions + Auth)              │
└─────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────┐
│                    Data Transformers                         │
│  snake_case (DB) ←→ camelCase (Frontend)                    │
└─────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────┐
│                    React Contexts                            │
│  (State Management + Business Logic + Real-time)            │
└─────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────┐
│                    React Components                          │
│  (UI + User Interactions)                                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 🗃️ Contexts vs Stores

### Contexts (20 context)
- **الغرض**: إدارة البيانات المتزامنة مع Supabase
- **الميزات**: Real-time subscriptions, CRUD operations
- **أمثلة**: `CardsContext`, `PrepaidCardsContext`, `BankAccountsContext`

### Stores (16 store)
- **الغرض**: حالة UI محلية وتخزين مؤقت
- **الميزات**: سريعة، لا تتصل بالـ DB مباشرة
- **أمثلة**: `useUIStore`, `useFilterStore`

### العلاقة بينهما
```typescript
// StoreInitializer يربط بين Contexts و Stores
// Contexts تجلب البيانات من Supabase
// Stores تخزن نسخة محلية للوصول السريع
```

---

## 🔧 الأنماط المستخدمة

### 1. Generic CRUD Hook
```typescript
const { items, create, update, remove } = useCrud<BankAccount>({
  tableName: 'bank_accounts',
  enableRealtime: true,
})
```

### 2. Data Transformers
```typescript
// من قاعدة البيانات للواجهة
const account = transformBankAccount(dbRecord)

// من الواجهة لقاعدة البيانات
const dbData = transformBankAccountToDB(account)
```

### 3. Error Handler
```typescript
try {
  await operation()
} catch (error) {
  handleError('Context.operation', error, true)
}
```

---

## 📊 جداول قاعدة البيانات

| الجدول | الوصف |
|--------|-------|
| `bank_accounts` | الحسابات البنكية |
| `credit_cards` | البطاقات الائتمانية |
| `prepaid_cards` | البطاقات المدفوعة مسبقاً |
| `transactions` | المعاملات المالية |
| `budgets` | الميزانيات |
| `savings_goals` | أهداف الادخار |
| `installments` | الأقساط |
| `loans` | القروض |

---

## 🧪 الاختبارات

```bash
# تشغيل جميع الاختبارات
npm run test

# تشغيل مرة واحدة
npm run test:run

# مع التغطية
npm run test:coverage
```

---

## 📝 ملاحظات مهمة

1. **snake_case vs camelCase**: قاعدة البيانات تستخدم `snake_case`، الواجهة تستخدم `camelCase`
2. **العملة الافتراضية**: الجنيه المصري (EGP)
3. **اللغة**: العربية (RTL)
4. **Real-time**: جميع الـ Contexts تدعم التحديث الفوري

